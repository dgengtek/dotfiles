% weather

# Show weather info for current location
curl -s "wttr.in" | rg -v -e "New feature" -e Follow

# Show weather info for a specific location
curl -s "wttr.in/<location>" | rg -v -e "New feature" -e Follow
