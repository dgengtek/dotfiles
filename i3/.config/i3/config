# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:NotoSans 14

# class                 border  backgr. text    indicator child_border
client.focused          #2cb5e9 #285577 #ffffff #2e9ef4   #285577
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty -e tmux
bindsym $mod+Shift+Return exec alacritty

# kill focused window
bindsym $mod+Shift+c kill

# start dmenu (a program launcher)
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+l focus right
bindsym $mod+j focus down
bindsym $mod+k focus up

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+l move right
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up

# split in horizontal orientation
bindsym $mod+minus split v

# split in vertical orientation
bindsym $mod+Shift+minus split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child


# reload the configuration file
bindsym $mod+Control+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym j resize shrink width 10 px or 10 ppt
  bindsym k resize grow height 10 px or 10 ppt
  bindsym l resize shrink height 10 px or 10 ppt
  bindsym odiaeresis resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym Control+c mode "default"
}

# bindsym $mod+r mode "resize"
bindsym $mod+r exec alacritty -e bash -i -c 'setsid --fork $(navi --print)'
#bindsym $mod+r exec urxvtc -geometry 80x42+0+0 -e ~/.config/i3/i3menu.sh 
#for_window [title="^i3menu.sh$"] floating enable

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  mode hide
  hidden_state hide
  status_command i3status
#  output DisplayPort-2
  tray_output primary
}
################################################################################
# custom
bindsym $mod+o move container to output right
bindsym $mod+n workspace next_on_output
bindsym $mod+p workspace prev_on_output
bindsym $mod+Control+j focus output left
bindsym $mod+Control+k focus output right

bindsym $mod+F1 exec alacritty -e vim ~/.config/i3/config

# media keyboard control
bindsym XF86AudioRaiseVolume exec audio_vol_up.sh
bindsym XF86AudioLowerVolume exec audio_vol_down.sh
bindsym XF86AudioMute exec audio_mute_toggle.sh

# screen lock
bindsym $mod+Control+minus exec i3lock -c 000000

# screenshots
bindsym $mod+Print exec screenshot.sh
bindsym $mod+Control+Print exec screenshot.sh -w
bindsym $mod+Shift+Print exec screenshot.sh -s

# options
force_focus_wrapping yes
workspace_auto_back_and_forth yes
workspace_layout tabbed

# Make the currently focused window a scratchpad
bindsym $mod+Shift+numbersign move scratchpad

# Show the first scratchpad window
bindsym $mod+numbersign scratchpad show

# switch to workspace mode
bindsym $mod+1 mode "$output0"
bindsym $mod+2 mode "$output1"

set $screen0 DisplayPort-2
set $screen1 DVI-D-0

set $ws01 "1:shells"
set $ws02 "2:surf"
set $ws03 "3:priv"
set $ws04 "4:work"
set $ws05 "5:media"
set $ws06 "6:etc"
set $ws07 "7:0"

set $ws11 "1:read"
set $ws12 "2:ssh"
set $ws13 "3:reference"
set $ws14 "4:services"
set $ws15 "5:wiki"
set $ws16 "6:1"
set $ws17 "7:1"

workspace $ws01 output $screen0
workspace $ws02 output $screen0
workspace $ws03 output $screen0
workspace $ws04 output $screen0
workspace $ws05 output $screen0
workspace $ws06 output $screen0
workspace $ws07 output $screen0

workspace $ws11 output $screen1
workspace $ws12 output $screen1
workspace $ws13 output $screen1
workspace $ws14 output $screen1
workspace $ws15 output $screen1
workspace $ws16 output $screen1
workspace $ws17 output $screen1

mode "$output0" {
  set $screen0 DisplayPort-2
  bindsym 1 workspace $ws01;mode "default"
  bindsym 2 workspace $ws02;mode "default"
  bindsym 3 workspace $ws03;mode "default"
  bindsym 4 workspace $ws04;mode "default"
  bindsym 5 workspace $ws05;mode "default"
  bindsym 6 workspace $ws06;mode "default"
  bindsym 7 workspace $ws07;mode "default"
  bindsym $mod+1 workspace $ws01;mode "default"
  bindsym $mod+2 workspace $ws02;mode "default"
  bindsym $mod+3 workspace $ws03;mode "default"
  bindsym $mod+4 workspace $ws04;mode "default"
  bindsym $mod+5 workspace $ws05;mode "default"
  bindsym $mod+6 workspace $ws06;mode "default"
  bindsym $mod+7 workspace $ws07;mode "default"
  bindsym Shift+1 move container to workspace $ws01;mode "default"
  bindsym Shift+2 move container to workspace $ws02;mode "default"
  bindsym Shift+3 move container to workspace $ws03;mode "default"
  bindsym Shift+4 move container to workspace $ws04;mode "default"
  bindsym Shift+5 move container to workspace $ws05;mode "default"
  bindsym Shift+6 move container to workspace $ws06;mode "default"
  bindsym Shift+7 move container to workspace $ws07;mode "default"
  bindsym $mod+Shift+1 move container to workspace $ws01;mode "default"
  bindsym $mod+Shift+2 move container to workspace $ws02;mode "default"
  bindsym $mod+Shift+3 move container to workspace $ws03;mode "default"
  bindsym $mod+Shift+4 move container to workspace $ws04;mode "default"
  bindsym $mod+Shift+5 move container to workspace $ws05;mode "default"
  bindsym $mod+Shift+6 move container to workspace $ws06;mode "default"
  bindsym $mod+Shift+7 move container to workspace $ws07;mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym Control+c mode "default"
}
mode "$output1" {
  bindsym 1 workspace $ws11;mode "default"
  bindsym 2 workspace $ws12;mode "default"
  bindsym 3 workspace $ws13;mode "default"
  bindsym 4 workspace $ws14;mode "default"
  bindsym 5 workspace $ws15;mode "default"
  bindsym 6 workspace $ws16;mode "default"
  bindsym 7 workspace $ws17;mode "default"
  bindsym $mod+1 workspace $ws11;mode "default"
  bindsym $mod+2 workspace $ws12;mode "default"
  bindsym $mod+3 workspace $ws13;mode "default"
  bindsym $mod+4 workspace $ws14;mode "default"
  bindsym $mod+5 workspace $ws15;mode "default"
  bindsym $mod+6 workspace $ws16;mode "default"
  bindsym $mod+7 workspace $ws17;mode "default"
  bindsym Shift+1 move container to workspace $ws11;mode "default"
  bindsym Shift+2 move container to workspace $ws12;mode "default"
  bindsym Shift+3 move container to workspace $ws13;mode "default"
  bindsym Shift+4 move container to workspace $ws14;mode "default"
  bindsym Shift+5 move container to workspace $ws15;mode "default"
  bindsym Shift+6 move container to workspace $ws16;mode "default"
  bindsym Shift+7 move container to workspace $ws17;mode "default"
  bindsym $mod+Shift+1 move container to workspace $ws11;mode "default"
  bindsym $mod+Shift+2 move container to workspace $ws12;mode "default"
  bindsym $mod+Shift+3 move container to workspace $ws13;mode "default"
  bindsym $mod+Shift+4 move container to workspace $ws14;mode "default"
  bindsym $mod+Shift+5 move container to workspace $ws15;mode "default"
  bindsym $mod+Shift+6 move container to workspace $ws16;mode "default"
  bindsym $mod+Shift+7 move container to workspace $ws17;mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym Control+c mode "default"
}
